# 视频生成向导重构实现计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 按照 PRD 4.2 重构视频生成向导，实现5步流程：选择IP→模特图→定妆图→首帧图→视频

**Architecture:** 重用现有向导框架，重构步骤组件和数据流。新增 POSE 素材类型。重构 API 支持新的生成函数。

**Tech Stack:** Next.js App Router, React, Framer Motion, Prisma

---

## 文件清单

### 新建文件
- `prisma/migrations/YYYYMMDDHHMMSS_add_pose_material_type/migration.sql`
- `prisma/migrations/YYYYMMDDHHMMSS_add_pose_material_type/migration_lock.toml`
- `app/api/products/[id]/model-image/route.ts` - 模特图生成 API
- `app/api/products/[id]/style-image/route.ts` - 定妆图生成 API
- `app/api/product-materials/route.ts` - 获取产品素材列表 API

### 修改文件
- `prisma/schema.prisma` - MaterialType 枚举添加 POSE
- `domains/materials/validators.ts` - createMaterialSchema 添加 POSE
- `domains/materials/types.ts` - MaterialType 添加 POSE
- `domains/video-generation/types.ts` - 新增 ModelImageGenerationResult, StyleImageGenerationResult
- `domains/video-generation/service.ts` - 新增 generateModelImage, generateStyleImage 函数
- `app/api/materials/route.ts` - GET 支持 POSE 类型
- `app/(app)/materials/page.tsx` - 添加 POSE 过滤选项
- `components/material/MaterialUploader.tsx` - 添加 POSE 选项
- `components/material/MaterialCard.tsx` - 添加 POSE 显示
- `app/(app)/products/[id]/GenerateVideoWizard.tsx` - 完全重构步骤组件

---

## Task 1: 数据库迁移 - 添加 POSE 素材类型

**Files:**
- Create: `prisma/migrations/20260423000000_add_pose_material_type/migration.sql`
- Create: `prisma/migrations/20260423000000_add_pose_material_type/migration_lock.toml`

- [ ] **Step 1: 创建迁移目录和文件**

```sql
-- Migration: add_pose_material_type
-- Created: 2026-04-23

ALTER TABLE `materials` MODIFY COLUMN `type` ENUM('SCENE', 'POSE', 'MAKEUP', 'ACCESSORY', 'OTHER') NOT NULL;
```

```toml
# Please do not edit this file manually
provider = "mysql"
```

Run:
```bash
mkdir -p prisma/migrations/20260423000000_add_pose_material_type
```

- [ ] **Step 2: 应用迁移**

Run: `npx prisma migrate deploy`
Expected: "All migrations have been successfully applied"

- [ ] **Step 3: 更新 domain 类型**

修改 `domains/materials/types.ts`:
```typescript
export type MaterialType = 'SCENE' | 'POSE' | 'MAKEUP' | 'ACCESSORY' | 'OTHER'
```

修改 `domains/materials/validators.ts`:
```typescript
type: z.enum(['SCENE', 'POSE', 'MAKEUP', 'ACCESSORY', 'OTHER'])
```

- [ ] **Step 4: 提交**

```bash
git add -A
git commit -m "feat: add POSE material type"
```

---

## Task 2: 新增视频生成 API 端点

**Files:**
- Create: `app/api/products/[id]/model-image/route.ts`
- Create: `app/api/products/[id]/style-image/route.ts`

- [ ] **Step 1: 创建 model-image API**

```typescript
import { NextRequest, NextResponse } from 'next/server'
import { getServerSession } from 'next-auth'
import { authOptions } from '@/foundation/lib/auth'
import { generateModelImage } from '@/domains/video-generation/service'

// POST /api/products/{id}/model-image
export async function POST(
  request: NextRequest,
  { params }: { params: { id: string } }
) {
  try {
    const session = await getServerSession(authOptions)
    if (!session?.user) {
      return NextResponse.json({ error: 'Unauthorized' }, { status: 401 })
    }

    const body = await request.json()
    const { ipId, productMainImageUrl, productDetailImageUrls } = body

    if (!ipId || !productMainImageUrl) {
      return NextResponse.json({ error: 'Missing required fields: ipId, productMainImageUrl' }, { status: 400 })
    }

    const result = await generateModelImage(
      params.id,
      ipId,
      productMainImageUrl,
      productDetailImageUrls || []
    )

    return NextResponse.json(result)
  } catch (error) {
    console.error('Model image generation error:', error)
    return NextResponse.json({ error: 'Internal server error' }, { status: 500 })
  }
}
```

- [ ] **Step 2: 创建 style-image API**

```typescript
import { NextRequest, NextResponse } from 'next/server'
import { getServerSession } from 'next-auth'
import { authOptions } from '@/foundation/lib/auth'
import { generateStyleImage } from '@/domains/video-generation/service'

// POST /api/products/{id}/style-image
export async function POST(
  request: NextRequest,
  { params }: { params: { id: string } }
) {
  try {
    const session = await getServerSession(authOptions)
    if (!session?.user) {
      return NextResponse.json({ error: 'Unauthorized' }, { status: 401 })
    }

    const body = await request.json()
    const { productMaterialId, pose, makeupUrl, accessoryUrl } = body

    if (!productMaterialId || !pose) {
      return NextResponse.json({ error: 'Missing required fields: productMaterialId, pose' }, { status: 400 })
    }

    const result = await generateStyleImage(
      productMaterialId,
      pose,
      makeupUrl,
      accessoryUrl
    )

    return NextResponse.json(result)
  } catch (error) {
    console.error('Style image generation error:', error)
    return NextResponse.json({ error: 'Internal server error' }, { status: 500 })
  }
}
```

- [ ] **Step 3: 提交**

```bash
git add -A
git commit -m "feat: add model-image and style-image API endpoints"
```

---

## Task 3: 新增 product-materials API

**Files:**
- Create: `app/api/product-materials/route.ts`

- [ ] **Step 1: 创建 API**

```typescript
import { NextRequest, NextResponse } from 'next/server'
import { getServerSession } from 'next-auth'
import { authOptions } from '@/foundation/lib/auth'
import { getProductMaterials } from '@/domains/product-material/service'

// GET /api/product-materials?productId={id}&ipId={ipId}
export async function GET(request: NextRequest) {
  try {
    const session = await getServerSession(authOptions)
    if (!session?.user) {
      return NextResponse.json({ error: 'Unauthorized' }, { status: 401 })
    }

    const searchParams = request.nextUrl.searchParams
    const productId = searchParams.get('productId')
    const ipId = searchParams.get('ipId')

    if (!productId) {
      return NextResponse.json({ error: 'Missing productId' }, { status: 400 })
    }

    const materials = await getProductMaterials({
      productId,
      ipId: ipId || undefined,
    })

    return NextResponse.json(materials)
  } catch (error) {
    console.error('Get product materials error:', error)
    return NextResponse.json({ error: 'Internal server error' }, { status: 500 })
  }
}
```

- [ ] **Step 2: 提交**

```bash
git add -A
git commit -m "feat: add product-materials API endpoint"
```

---

## Task 4: 更新前端素材组件支持 POSE

**Files:**
- Modify: `app/(app)/materials/page.tsx` - 添加 POSE 过滤选项
- Modify: `components/material/MaterialUploader.tsx` - 添加 POSE 选项
- Modify: `components/material/MaterialCard.tsx` - 添加 POSE 显示

- [ ] **Step 1: 更新 materials/page.tsx**

在 typeOptions 数组中添加 POSE：
```typescript
{ value: 'POSE', label: '姿势', gradient: 'from-orange-500/20 to-red-500/20', accent: '#fb923c' },
```

- [ ] **Step 2: 更新 MaterialUploader.tsx**

在 formData 初始化中: `type: 'SCENE'`
在 typeOptions 中添加:
```typescript
{ value: 'POSE', label: '姿势', icon: '🧍' },
```

- [ ] **Step 3: 更新 MaterialCard.tsx**

在 typeColors 中添加:
```typescript
POSE: 'from-orange-500/20 to-red-500/20',
```

在类型显示中添加:
```typescript
material.type === 'POSE' ? '姿势' :
```

- [ ] **Step 4: 提交**

```bash
git add -A
git commit -m "feat: add POSE material type support to frontend"
```

---

## Task 5: 重构 GenerateVideoWizard

**Files:**
- Modify: `app/(app)/products/[id]/GenerateVideoWizard.tsx`

这是最大的改动，需要完全重构向导组件。

### 新的步骤定义

```typescript
const STEPS = [
  { id: 'select-ip', label: '选择虚拟IP', icon: '🎭' },
  { id: 'model-image', label: '模特图', icon: '👗' },
  { id: 'style-image', label: '定妆图', icon: '💄' },
  { id: 'first-frame', label: '首帧图', icon: '🖼️' },
  { id: 'video', label: '生成视频', icon: '🎥' },
]
```

### 新的状态管理

```typescript
// Step 1: IP Selection
const [ips, setIps] = useState<VirtualIP[]>([])
const [selectedIp, setSelectedIp] = useState<VirtualIP | null>(null)

// Step 2: Model Image
const [productMainImage, setProductMainImage] = useState<string | null>(null)
const [productDetailImages, setProductDetailImages] = useState<string[]>([])
const [existingModelImage, setExistingModelImage] = useState<string | null>(null)
const [modelImageUrl, setModelImageUrl] = useState<string | null>(null)
const [modelImageLoading, setModelImageLoading] = useState(false)
const [useExistingModel, setUseExistingModel] = useState(false)

// Step 3: Style Image
const [existingStyleImages, setExistingStyleImages] = useState<string[]>([])
const [selectedPose, setSelectedPose] = useState<Material | null>(null)
const [selectedMakeup, setSelectedMakeup] = useState<Material | null>(null)
const [selectedAccessory, setSelectedAccessory] = useState<Material | null>(null)
const [styledImageUrl, setStyledImageUrl] = useState<string | null>(null)
const [styleImageLoading, setStyleImageLoading] = useState(false)
const [useExistingStyle, setUseExistingStyle] = useState(false)

// Step 4: First Frame
const [existingFirstFrames, setExistingFirstFrames] = useState<string[]>([])
const [selectedScene, setSelectedScene] = useState<Material | null>(null)
const [compositionText, setCompositionText] = useState('')
const [firstFrameUrl, setFirstFrameUrl] = useState<string | null>(null)
const [firstFrameLoading, setFirstFrameLoading] = useState(false)
const [useExistingFirstFrame, setUseExistingFirstFrame] = useState(false)

// Step 5: Video
const [movements, setMovements] = useState<Movement[]>([])
const [selectedMovement, setSelectedMovement] = useState<Movement | null>(null)
const [videoUrl, setVideoUrl] = useState<string | null>(null)
const [videoLoading, setVideoLoading] = useState(false)
const [videoProgress, setVideoProgress] = useState(0)
```

### 互斥逻辑

每一步的"选择已有"和"生成"互斥：

```typescript
// 选择已有 → 禁用生成按钮
// 点击生成 → 清空已有选择

const handleSelectExistingModel = (url: string) => {
  setExistingModelImage(url)
  setUseExistingModel(true)
  setModelImageUrl(null)
}

const handleGenerateModel = async () => {
  setUseExistingModel(false)
  setExistingModelImage(null)
  // 调用生成 API
}
```

### 步骤组件重构

每个步骤组件接收统一的 props：
- `mode: 'select' | 'generate'` - 当前模式
- `onModeChange: (mode: 'select' | 'generate') => void` - 模式切换

```typescript
// Step 2: ModelImageStep
interface ModelImageStepProps {
  mode: 'select' | 'generate'
  onModeChange: (mode: 'select' | 'generate') => void
  existingModelImage: string | null
  onSelectExisting: (url: string) => void
  productMainImage: string | null
  onProductMainSelect: (url: string) => void
  productDetailImages: string[]
  onProductDetailsSelect: (urls: string[]) => void
  modelImageUrl: string | null
  isLoading: boolean
  onGenerate: () => void
}

// Step 3: StyleImageStep
interface StyleImageStepProps {
  mode: 'select' | 'generate'
  onModeChange: (mode: 'select' | 'generate') => void
  existingStyleImages: string[]
  onSelectExisting: (url: string) => void
  poses: Material[]
  selectedPose: Material | null
  onPoseSelect: (pose: Material) => void
  makeups: Material[]
  selectedMakeup: Material | null
  onMakeupSelect: (makeup: Material) => void
  accessories: Material[]
  selectedAccessory: Material | null
  onAccessorySelect: (accessory: Material) => void
  styledImageUrl: string | null
  isLoading: boolean
  onGenerate: () => void
}

// Step 4: FirstFrameStep
interface FirstFrameStepProps {
  mode: 'select' | 'generate'
  onModeChange: (mode: 'select' | 'generate') => void
  existingFirstFrames: string[]
  onSelectExisting: (url: string) => void
  scenes: Material[]
  selectedScene: Material | null
  onSceneSelect: (scene: Material) => void
  compositionText: string
  onCompositionChange: (text: string) => void
  firstFrameUrl: string | null
  isLoading: boolean
  onGenerate: () => void
}

// Step 5: VideoStep
interface VideoStepProps {
  movements: Movement[]
  selectedMovement: Movement | null
  onMovementSelect: (movement: Movement) => void
  videoUrl: string | null
  isLoading: boolean
  progress: number
  onGenerate: () => void
}
```

### canProceed 逻辑

```typescript
const canProceed = () => {
  switch (currentStep) {
    case 0: return !!selectedIp
    case 1: return useExistingModel ? !!existingModelImage : !!modelImageUrl
    case 2: return useExistingStyle ? !!styledImageUrl : !!styledImageUrl
    case 3: return useExistingFirstFrame ? !!firstFrameUrl : !!firstFrameUrl
    case 4: return !!videoUrl
    default: return false
  }
}
```

### 数据获取 useEffect

```typescript
useEffect(() => {
  // 步骤1: 获取 IP 列表
  fetchIPs()

  // 步骤3: 获取姿势、妆容、饰品列表 (POSE 类型)
  fetchPoses()
  fetchMakeups()
  fetchAccessories()

  // 步骤4: 获取场景列表 (SCENE 类型)
  fetchScenes()

  // 步骤5: 获取动作列表
  fetchMovements()
}, [])

// 获取已有数据 (当 selectedIp 改变时)
useEffect(() => {
  if (selectedIp && productId) {
    fetchExistingMaterials()
  }
}, [selectedIp, productId])

const fetchExistingMaterials = async () => {
  const res = await fetch(`/api/product-materials?productId=${productId}&ipId=${selectedIp?.id}`)
  if (res.ok) {
    const data = await res.json()
    // 提取已有的模特图、定妆图、首帧图
    setExistingModelImage(data[0]?.fullBodyUrl || null)
    setExistingStyleImages(data.map((d: any) => d.fullBodyUrl).filter(Boolean))
    setExistingFirstFrames(data.map((d: any) => d.firstFrameUrl).filter(Boolean))
  }
}
```

- [ ] **Step 1: 重构状态管理**

修改 `GenerateVideoWizard` 组件的 state 和 useEffect

- [ ] **Step 2: 重构步骤渲染逻辑**

修改步骤渲染，适配新的5步流程

- [ ] **Step 3: 重构每个步骤组件**

- `SelectIPStep` - 保持不变
- `ModelImageStep` - 新建，处理产品图选择和模特图生成
- `StyleImageStep` - 新建，处理姿势/妆容/饰品选择和定妆图生成
- `FirstFrameStep` - 新建，处理场景选择+构图输入和首帧图生成
- `VideoStep` - 新建，处理动作选择和视频生成

- [ ] **Step 4: 更新 canProceed 和导航逻辑**

- [ ] **Step 5: 提交**

```bash
git add -A
git commit -m "refactor: redesign video generation wizard with 5-step flow"
```

---

## Task 6: 更新首页工具链接

**Files:**
- Modify: `app/(app)/page.tsx`

- [ ] **Step 1: 更新工具卡片的 href**

将所有工具卡片的 href 从 `/products` 更新为具体的视频生成入口

```typescript
{
  id: '1',
  title: '双图编辑',
  href: '/products', // 或保留为产品列表页
},
```

实际上首页的工具卡片目前只是展示用的，链接到 `/products` 是合理的。不需要修改。

- [ ] **Step 2: 提交**

```bash
git add -A
git commit -m "docs: update home page tool links"
```

---

## 执行顺序

1. Task 1: 数据库迁移 - 添加 POSE 类型
2. Task 2: 新增 model-image 和 style-image API
3. Task 3: 新增 product-materials API
4. Task 4: 更新前端素材组件支持 POSE
5. Task 5: 重构 GenerateVideoWizard
6. Task 6: 更新首页工具链接（可选）
